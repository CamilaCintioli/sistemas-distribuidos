-module(pqueue).
-export([new/0,queue/3,split/2,is_empty/1,dequeue/1]).

new() ->
    ok.

queue(Priority, Value, Queue) ->
    ok.

split(Priority, Queue) ->
    ok.

is_empty(Queue) ->
    ok.

dequeue(Queue) ->
    ok.
